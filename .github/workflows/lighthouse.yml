name: Lighthouse Audit
on: [push]

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Audit live site
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: "https://air.siemreap.cloud"
          temporaryPublicStorage: true
